# TEST-ONLY 渲染输入

本目录只保留离线验收器直接依赖的固定输入，不是法院提交目录。

- `render-cleaning-source/render.pdf`：验收器使用的固定 PDF 输入；仅用于检查组卷、书签、页码和原件不变。
- 历史生成样件和逐页渲染图已经移出活动源码；需要时由验收流程重新生成。

清洁派生件即使预检无阻断项，仍保持 `rereview_status=required`。所有文件均不得用于真实案件或提交。
