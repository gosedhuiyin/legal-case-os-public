# 范围锁与外部动作

读取范围、联网范围和允许修改范围来自当前 `IntentEnvelope`，不能因“顺便”“整理一下”自动扩大。局部修改只改 `modify_scope` 指定部分，并通过最小修改差异检查保护其余内容。

“整理文件夹”仅允许在新的候选目录中复制或生成派生件、清单和索引；不得移动、删除、覆盖或重命名原始材料。

本源码包不执行打印、发送、登录、上传、送达或提交。打印生产单只是候选清单。任何外部动作都必须另有G5批准，绑定精确G4包哈希、动作和目标；本阶段即使记录了G5也不提供执行器。
