# v1.2 当前总体架构图

可缩放、可编辑的架构源图见 [architecture.svg](./architecture.svg)；重新生成源图的脚本见 [tools/generate_architecture.py](./tools/generate_architecture.py)。

## 读图要点

1. 四块稳定内核分别是自然语言总控、案件内状态、个人模板学习，以及法源约束下的文书合成。对话窗口先把当前原话编译为瞬时 `TaskFrame → clarification → RunSpec → IntentEnvelope/FocusContext`；这些控制对象只约束本次执行，不是案件事实。
2. 每次调用都冻结一种记忆读取模式：无案件状态默认关闭；有明确案件时才可选择文件限定、任务相关或触发式反思。读取记忆本身不会授权写入，写入另分为不写、候选和律师确认提交。
3. 首次入库继续走“预处理—索引—标准/深析—法源/范例/证据—起草—审阅—清洁—组卷”的主线；简单案连续推进，复杂案只下钻危险争点。
4. `legal-template-curator` 把模板候选蒸馏为 `FormProfile` 或 `WritingProfile`。原模板始终只读；候选画像和渲染基线保存在 `_profiles`，未经律师批准不会进入正式模板目录，也不能按别名调用。
5. 手续采用 `FillPlan` 逐格绑定值、来源和留空原因，再复制完整 DOCX、只修改已批准文字节点。收费方式、收费金额、代理权限等高风险字段必须由律师决定，签章和现场日期等 `manual_blank` 不得自动填写。标准行克隆和整块正文替换已有 TEST-ONLY 结构引擎，但真实模板须先登记稳定锚点并通过视觉批准。
6. 复杂文书由持久 `CompositionSpec` 冻结目标、主辅模板、争点、证据、法源和逐命题 `ClaimBinding`。版式主模板一份、结构主范例一份、分段辅助范例最多三份；当前案已核验事实和法源始终高于旧模板。
7. 引用审计阻断未登记案号和法条；防串案检查用画像中的哈希指纹阻断旧案姓名、案号、金额和特征表述。画像、风险记录和审阅结果留在内部，正式 DOCX 只保留干净对外正文。
8. 后续新增邮件、传票或证据先形成 `MaterialBatch`，只扫描本批材料，再由分诊卡分到“无动作、期限、影响原案、客户指令或弱信号新案”路径。上游模板、法源、争点或证据变化时，相关合成计划、文书和包快照一并失效。
9. 撤诉、保全、调取审计报告等事项仍由持久 `WorkflowInstance` 管理；单次 AI 或脚本运行记为可失败、可重试的 `RunAttempt`，运行结束不等于工单完成。
10. 法院候选须经过 G2 证据门禁、法源与串案审计、独立审阅、清洁双审和 G4 精确包快照；无 API、会员或网络时明确降级，G5 外部动作始终由律师另行授权，本版本不执行。

## 当前成熟度

这是一份源码完成候选总图，不是生产启用声明。v1.2 已用虚构 DOCX 验证 `repeatable` 标准行克隆、`hybrid` 整块正文替换及空值/长名称/大额金额边界；这些结构命令仍只允许 TEST-ONLY。首批真实模板画像仍须人工标定锚点、修复已发现的版式问题并逐页批准，真实案件和复杂组合仍须回放。Claude、GPT 和千问等平台适配属于下一阶段。
